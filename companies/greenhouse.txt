# Greenhouse board tokens to crawl.
#
# One per line. The token is the slug in the board URL:
#
#     https://boards.greenhouse.io/ramp   ->   ramp
#     https://job-boards.greenhouse.io/figma   ->   figma
#
# Public JSON, no login, no rate limit worth worrying about — add freely.
#
# A wrong token returns a 404 that looks exactly like a company with no
# openings, so the status panel calls it out by name. If a line shows "HTTP 404 —
# no such board token", the company either renamed its board or moved off
# Greenhouse; open the careers page and re-copy the slug.
#
# This file wins over anything saved in the settings page — that is why the
# settings field is read-only. Edit here; the next crawl picks it up, no restart.

# ---- from your watchlist; every token below was verified against the API ----
razorpaysoftwareprivatelimited   # Razorpay
towerresearchcapital             # Tower Research Capital
gravitonresearchcapital          # Graviton Research Capital
arcesiumllc                      # Arcesium

# Innovaccer: job-boards.greenhouse.io/innovaccer renders ~85 jobs in a browser,
# but the public board API 404s for every spelling tried (innovaccer,
# innovaccerinc, innovaccerhq). Their board is not exposed through the API, so
# there is nothing to crawl — left out rather than left in as a permanent 404.

# ---- general seeds; delete if you don't care about them ----
stripe
gitlab

# Adding more: open <company>.com/careers and look for a boards.greenhouse.io or
# job-boards.greenhouse.io link — the slug after the last / is the token. Check
# it with:
#   curl -s https://boards-api.greenhouse.io/v1/boards/<token>/jobs | head -c 200
